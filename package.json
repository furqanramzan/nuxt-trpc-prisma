{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "cp .env.example .env -n && nuxt prepare && prisma generate"
  },
  "devDependencies": {
    "@nuxtjs/web-vitals": "^0.2.2",
    "@prisma/client": "^4.10.1",
    "@trpc/client": "^10.12.0",
    "@trpc/server": "^10.12.0",
    "nuxt": "^3.2.2",
    "prisma": "^4.10.1",
    "trpc-nuxt": "^0.7.0",
    "zod": "^3.20.6"
  },
  "prisma": {
    "schema": "server/prisma/schema.prisma"
  }
}
